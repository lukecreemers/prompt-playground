{
  "name": "maestro-testing",
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": ["@nestjs/core", "better-sqlite3", "esbuild"]
  },
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "dev:backend": "pnpm --filter maestro-testing-backend run start:dev",
    "dev:frontend": "pnpm --filter maestro-testing-frontend run dev",
    "build": "pnpm -r run build",
    "build:backend": "pnpm --filter maestro-testing-backend run build",
    "build:frontend": "pnpm --filter maestro-testing-frontend run build"
  }
}
